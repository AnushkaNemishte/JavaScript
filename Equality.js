//Both == and === are used for comparision .
//== is used for comparing two variables but it ignores datatype.
// === is used for comparision and also checks the datatype

let x = 40
let y = '40'
console.log("==",x==y)
console.log("===",x===y);